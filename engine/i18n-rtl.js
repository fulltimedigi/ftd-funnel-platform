/**
 * engine/i18n-rtl.js — RTL + localization helpers.
 *
 * Responsibility (§5):
 *   Arabic RTL is first-class. Provide: Western→Arabic numeral conversion,
 *   dir handling, and LTR overrides for email/phone fields inside an RTL page.
 *
 * Exposes (planned): toArabicNumerals(n), applyDir(el, dir), ltrField(el).
 *
 * STATUS: foundation stub. No logic implemented yet.
 */

// TODO(v0.1): implement numeral conversion + dir helpers.
export function toArabicNumerals() {
  throw new Error("engine/i18n-rtl.js: not implemented (foundation stub)");
}
